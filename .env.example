# `.env` is gitignored.

# Where raw downloads + processed graph cache live 
GNOME_DATA_DIR=./data

# Where training outputs go (checkpoints + logs)
GNOME_RUNS_DIR=./runs

# 'cuda' on collaborators box, 'cpu' on my laptop
GNOME_DEVICE=cpu

# Optional: W&B project name
WANDB_PROJECT=gnome-repro